/**
 * Native (iOS / Android) stub — marketing pixels are browser-only.
 * For native attribution use @react-native-firebase/analytics (already
 * partially wired via firebase-config.js) and AppsFlyer when running
 * paid UA campaigns.
 */
export default function MarketingPixels() {
  return null;
}
